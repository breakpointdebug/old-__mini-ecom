import { Module } from '@nestjs/common';

//TODO:
// unit tests after guards, authentication and authorization has been completed
// why? because we need some form of testing to make sure nothing breaks
// just use a different local instance of a database during tests

@Module({})
export class AccountModule { }
